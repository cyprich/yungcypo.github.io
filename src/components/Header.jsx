import React from 'react';

const Header = () => {
    return (
        <nav className={"flex h-16 w-full bg-black fixed top-0 left-0 z-50 text-gray-50 "}>
            <p>Header</p>
        </nav>
    );
};

export default Header;