import React from 'react';

const CasovaOsElement = ({id, nadpis, podnadpis, popis, obrazky, datum, datumnakoniec, align}) => {
    return (
        <li>

        </li>
    );
};

export default CasovaOsElement;